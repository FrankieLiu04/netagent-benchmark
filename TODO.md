# TODO

- Run `llm-smoke` with a real OpenAI-compatible provider key.
- Run `tools` and `doctor` against the internal CML MCP environment.
- Run `run "list all CML labs"` against real LLM and CML MCP services.
- Post a generated `run.json` with `NETAGENT_WORKBENCH_EXPERIMENT_ID` set to the workbench import API.
